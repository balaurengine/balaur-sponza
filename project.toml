[application]
name = "sponza"
main_scene = "scenes/main.toml"

# The size the clip and the still are rendered at offscreen, which is what the
# website's player expects.
[window]
width = 1920
height = 1080
