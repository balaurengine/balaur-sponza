# The Khronos model and what `balaur import` makes of it: 50 MB of somebody
# else's art. `scripts/fetch.py` pulls it back.
source/
models/

# Written by scripts/stage.py from the imported scene.
scenes/main.toml

# Written by scripts/media.sh from a run; the .webm and .mp4 are committed.
